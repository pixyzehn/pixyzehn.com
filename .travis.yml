language: ruby
rvm: 2.6.0

before_install:
  - gem --version
  - gem update --system
  - gem install bundler

script:
  - bundle exec jekyll build

branches:
  only:
    - master

sudo: false
cache: bundler
